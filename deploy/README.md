# Deploy files

Files applied to the server as-is:

- `order-lookup.service` → `/etc/systemd/system/order-lookup.service`
- `nginx.conf` → `/etc/nginx/sites-available/order-lookup`

See top-level `README.md` for the full deploy runbook.
